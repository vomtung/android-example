<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vmtung.example">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PopoverExampleActivity" />
        <activity
            android:name=".MenuDrawerActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".FrameLayoutExampleActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".AlerDialogExampleActivity" />
        <activity android:name=".CompoundViewActivity" />
        <activity android:name=".CustomViewActivity" />
        <activity android:name=".CanvasAndPaintExampleActivity" />
        <activity android:name=".CanvasAndPaint2Activity" />
        <activity android:name=".CanvasAndPaint3Activity" />
        <activity android:name=".CanvasAndPaintExample4Activity" />
        <activity
            android:name=".ListViewActivity"
            android:label="@string/title_activity_list_view"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>